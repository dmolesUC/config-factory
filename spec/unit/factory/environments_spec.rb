require 'spec_helper'

module Factory
  module Factory
    describe Environments do
      describe '#load' do
        it 'defaults to a single environment'
        it 'auto-detects standard environments'
        it 'accepts additional environments'
      end
    end
  end
end
